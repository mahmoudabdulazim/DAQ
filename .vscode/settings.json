// Place your settings in this file to overwrite default and user settings.
{
    "clang.completion.triggerChars": [
        ".",
        ":",
        ">",
        "<"
    ],
    // Compiler options for C++ (e.g. ['-std=c++11'])
    "clang.cxxflags": [
    "-I/usr/local/include/opencv" // header files for oblong
  ]
}