#include <opencv2/core/core.hpp>
#include <iostream>
#include <string>

class Camera
{
	public:
		FileStorage CalibrData;
		FileStorage CharacteristicsFile;
		Mat IntrinsicParameters;
		Mat ExtrinsicParameters;
		Mat DistortionCoefficients;
		int ImageWidth;
		int ImageHeight;
		Size ImageSize;
		double FoVx;
		double FoVy;
		double ApertureWidth;
		double ApertureHeight;
		double FocalLength;
		double AspectRatio;
		Point2d PrincipalPoint;

		void Intrinsic2Characteristics()
		{
			if (!(IntrinsicParameters.empty()))
			{
				calibrationMatrixValues(IntrinsicParameters,ImageSize,ApertureWidth,ApertureHeight,FoVx,FoVy,FocalLength,PrincipalPoint,AspectRatio);
			}
			else
			{
				cout << "Invalid Camera Matrix" << endl;
			}

		}

		uint_8 readCalibrationOutput(const string& source)
		{
			if ()
			{
				return 1;
			}
			else
			{
				return 0;
			}
		}

		void SaveParameters(const string& Source)
		{
			CharacteristicsFile = FileStorage(Source,FileStorage::WRITE);

			FileStorage::WRITE
		}

};
